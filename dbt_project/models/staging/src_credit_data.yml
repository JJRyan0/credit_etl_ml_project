version: 2

sources:
  - name: raw
    tables:
      - name: raw_credit_data
      - name: raw_credit_data
        description: "Raw credit data loaded from CSV"
        columns:
          - name: customer_id
            tests:
              - not_null
              - unique
